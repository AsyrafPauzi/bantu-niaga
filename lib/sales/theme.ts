import { PILLAR_COLORS, pillarClasses } from "@/lib/pillars/theme";

/** Sales pillar brand — re-exported from central pillar theme. */
export const SALES_BLUE = PILLAR_COLORS.sales.primary;
export const SALES_BLUE_DARK = PILLAR_COLORS.sales.dark;

export const salesClasses = pillarClasses.sales;
