import { PILLAR_COLORS, pillarClasses } from "@/lib/pillars/theme";

/** HR pillar brand — re-exported from central pillar theme. */
export const HR_TEAL = PILLAR_COLORS.hr.primary;
export const HR_TEAL_DARK = PILLAR_COLORS.hr.dark;

export const hrClasses = pillarClasses.hr;
